Same 72 combination search as discover_equation.py, with one change, the inner validation year used while the search itself is running no longer overlaps with the year used afterward to pick the winning combination. Previously both steps partly used 2019, so the later pick was not fully independent of the search. Now the search only ever sees 2018, and 2019 is seen for the first time when the winner is chosen. Reads the already built latent state, encoder, and province clusters from the parent folder rather than needing its own copy, and writes its own search results and winning program separately, so it never overwrites the original run.

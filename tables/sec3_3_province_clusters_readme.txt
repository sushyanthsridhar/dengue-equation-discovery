Which of the 32 provinces fall into which of the 5 latent-space clusters
(k-means on each province's mean 10-dim latent vector, k chosen by silhouette
score). Provinces marked "(excluded)" are the 4 border provinces dropped before
model fitting; they are shown here only because the clustering itself was
computed on all 32. Matches Table 3 (Section 3.3, Province Clustering) in the paper.

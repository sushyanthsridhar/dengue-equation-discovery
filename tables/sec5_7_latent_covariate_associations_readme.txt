For each of the 4 anonymous latent dimensions that the LLM proposer selected
into the final equation in at least one stability run, its top 3 correlations
against named raw covariates (training years only). This is how an opaque
latent number (e.g. z6) gets read as a real-world signal (e.g. temperature).
Matches Table 10 (Section 5.6, Associations Between Selected Latent Factors)
in the paper.
